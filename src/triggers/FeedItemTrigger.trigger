trigger FeedItemTrigger on FeedItem (after insert) {
   /*
   Trigger_FeedItem fi = new Trigger_FeedItem();

    if (!Settings.triggerSettings.DISABLE_ChatterPUBAutoTopicTagging__c && trigger.isAfter) {
        fi.chatterPUBAutoTopicTagging(trigger.new);
    } 

    if (!Settings.triggerSettings.DISABLE_AutoQuestionTopic__c && trigger.isAfter) {
        fi.attachQuestionTopic(trigger.new);
    }
    */
}